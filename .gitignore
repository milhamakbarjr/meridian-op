# Dependencies
node_modules/

# Environment & secrets
.env
.env.*
!.env.example
.envrypt

# User config (contains RPC URL, wallet key, API keys)
user-config.json
gmgn-config.json

# DRY_RUN sim data
dryrun-positions.json
dryrun-snapshots.jsonl
dryrun-closed.jsonl
dryrun-report-*.md
dryrun/cache/

# GMGN fee-source config (contains API key)
gmgn-config.json

# Stray Claude Code worktrees
.claude/worktrees/

# Runtime state (position tracking, lessons — user-specific)
state.json
lessons.json
smart-wallets.json
pool-memory.json
token-blacklist.json
strategy-library.json
decision-log.json
hivemind-cache.json
signal-weights.json

# Logs
logs/

# OS
.DS_Store

# Dev context
context.md
CONTEXT.md
dev-blocklist.json
docs/

# Test files
test-*.js

# Discord listener queue
discord-signals.json

# Playwright MCP browser logs
.playwright-mcp/

# Claude Code runtime lock
.claude/scheduled_tasks.lock

# Process PID files
*.pid
meridian.pid

# User-config backups / snapshots
user-config.json.*

# Local archive (session reports, audits, PR docs)
archive/
